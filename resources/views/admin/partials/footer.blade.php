<div class="container">
  <div class="row align-items-center">
    <div class="col-12 text-center">
      {{ __('common.copyright') }} &copy; {{ date('Y') }} <a href="{{ route('admin.dashboard') }}">{{ env('APP_NAME') }}</a>
    </div>
  </div>
</div>
